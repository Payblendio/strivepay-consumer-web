import type {ReactNode} from "react";

/** Auth and chrome live in `(shell)` / `(desk)` route-group layouts. */
export default function DashboardLayout({children}:{children:ReactNode}){
  return children;
}
