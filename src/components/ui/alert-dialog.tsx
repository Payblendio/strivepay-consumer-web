"use client";
import {Modal} from "./modal";
export function AlertDialog({open,onClose,onConfirm,title,message,tone="info",confirmLabel="Continue",busy=false}:{open:boolean;onClose:()=>void;onConfirm:()=>void;title:string;message:string;tone?:"info"|"success"|"warning"|"danger";confirmLabel?:string;busy?:boolean}){return <Modal open={open} onClose={onClose} title="" size="small"><div className="alert-content"><div className={`alert-symbol ${tone}`}>{tone==="success"?'✓':tone==="warning"?'!':tone==="danger"?'×':'i'}</div><h2>{title}</h2><p>{message}</p><div className="alert-actions"><button className="soft-button" onClick={onClose}>Cancel</button><button className={`solid-button ${tone}`} onClick={onConfirm} disabled={busy}>{busy?'Please wait':confirmLabel}</button></div></div></Modal>}
